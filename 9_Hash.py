#!/usr/bin/env python3

# Sets and hashes

hero = "Harry"
friend = "Ron"
heroine = "Hermione"

print(hash(hero))
print(hash(friend))
print(hash(heroine))

# Can we create a set of strings?
set1 = {hero, friend, heroine}
print(set1)
